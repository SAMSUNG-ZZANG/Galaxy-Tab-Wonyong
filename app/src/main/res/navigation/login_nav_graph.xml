<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/login_nav_graph"
    app:startDestination="@id/sign_in_fragment">

    <fragment
        android:id="@+id/sign_up_fragment"
        android:name="com.example.sopt_seminar.ui.signup.SignUpFragment"
        android:label="sign_up_activity"
        tools:layout="@layout/sign_up_fragment">
        <action
            android:id="@+id/action_sign_up_fragment_to_sign_in_fragment"
            app:destination="@id/sign_in_fragment"
            app:popUpTo="@id/sign_in_fragment"
            app:popUpToInclusive="true">
            <argument
                android:name="userId"
                android:defaultValue=""
                app:argType="string" />
            <argument
                android:name="userPassword"
                android:defaultValue=""
                app:argType="string" />
        </action>
    </fragment>

    <fragment
        android:id="@+id/sign_in_fragment"
        android:name="com.example.sopt_seminar.ui.signin.SignInFragment"
        android:label="sign_in_activity"
        tools:layout="@layout/sign_in_fragment">
        <argument
            android:name="userId"
            android:defaultValue=""
            app:argType="string" />
        <argument
            android:name="userPassword"
            android:defaultValue=""
            app:argType="string" />
        <action
            android:id="@+id/action_sign_in_fragment_to_sign_up_fragment"
            app:destination="@id/sign_up_fragment" />
        <action
            android:id="@+id/action_sign_in_fragment_to_nav_graph"
            app:destination="@id/nav_graph" />
    </fragment>
    <include app:graph="@navigation/main_nav_graph" />
</navigation>